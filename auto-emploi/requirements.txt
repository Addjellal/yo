# IA providers (installez selon votre choix)
anthropic>=0.69.0   # requis si PROVIDER=anthropic (Claude Fable 5 + sorties structurées)
ollama>=0.4.0       # requis si PROVIDER=ollama

# Configuration
python-dotenv>=1.0.0

# Parsing CV
pdfplumber>=0.11.0
pymupdf>=1.24.0
python-docx>=1.1.0

# Scraping
requests>=2.32.4     # >=2.32.4 : correctif CVE-2024-47081 (fuite credentials .netrc)
beautifulsoup4>=4.12.0
cloudscraper>=1.2.60  # contournement anti-bot Cloudflare (Indeed, Talent.com) — v1.2.x sur PyPI
defusedxml>=0.7.1    # parsing XML durci (flux RSS Indeed : anti-XXE/billion laughs)

# Automatisation navigateur (LinkedIn)
playwright>=1.47.0

# Interface CLI
rich>=13.9.0

# Génération PDF lettres de motivation
fpdf2>=2.8.0

# Notifications desktop (mode --watch) — optionnel
plyer>=2.1.0

# Tests
pytest>=8.0.0
